name: RestAssured Enterprise API Suite

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:
    inputs:
      api_url:
        description: 'Target API Base URL'
        default: 'https://api.stage.us-east-2.api.revdoc.link'
        type: string

# Global permissions for reporting and hosting
permissions:
  contents: read
  pages: write
  id-token: write
  checks: write
  pull-requests: write

jobs:
  build_verification:
    name: Build & Dockerize
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven
      - name: Compile & Build Image
        run: |
          mvn clean compile -DskipTests
          docker build -t api-automation-suite:${{ github.sha }} .

  execute_api_tests:
    name: Execute RestAssured Tests
    needs: build_verification
    runs-on: ubuntu-latest
    env:
      API_BASE_URL: ${{ github.event.inputs.api_url || secrets.STAGE_API_URL || 'https://api.stage.us-east-2.api.revdoc.link' }}
      AUTH_TOKEN: ${{ secrets.API_BEARER_TOKEN }}
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      - name: Execute API Tests
        run: mvn test -B -Dapi.base.url=${{ env.API_BASE_URL }}
        continue-on-error: true

      # 1. Rename the Timestamped report to index.html
      # This ensures the GitHub Pages link opens the most recent report automatically.
      - name: Prepare HTML Report for Deployment
        if: always()
        run: |
          # Find the newest .html file in the target folder and copy it to index.html
          latest_report=$(ls -t target/*.html | head -1)
          cp "$latest_report" target/index.html
          echo "Published $latest_report as index.html"

      # 2. Dashboard Summary (The quick table in GitHub UI)
      - name: Publish Test Results Summary
        uses: dorny/test-reporter@v1
        if: always()
        with:
          name: API Test Results Summary
          path: 'target/surefire-reports/junitreports/TEST-*.xml'
          reporter: java-junit

      # 3. Prepare HTML for Web Hosting
      - name: Upload Pages Artifact
        if: always()
        uses: actions/upload-pages-artifact@v3
        with:
          # Points to the folder where we just created index.html
          path: 'target/'

  deploy_report:
    name: Deploy Full HTML Report
    needs: execute_api_tests
    # Only deploy to pages when running on the main branch to avoid security errors
    if: always() && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4