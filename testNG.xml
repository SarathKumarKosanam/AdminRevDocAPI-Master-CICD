<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE suite SYSTEM "https://testng.org/testng-1.0.dtd">
<suite name="Admin API Regression Suite" verbose="1">

    <test name="Admin Functional Tests - All Classes in Package">
        <packages>
            <!-- 
                FIX: Package name updated to 'com.rev.admin.tests' as verified 
                to be the correct location for your APITest/UITest classes.
                This prevents the "Total tests run: 0" error in CI/CD.
            -->
            <package name="com.rev.admin.tests"/>
        </packages>
    </test>

    <!-- 
        NOTE on CI/CD: The CI pipeline uses the command 'mvn test -Dgroups=API' 
        to successfully filter and run only the API tests without modification to the YML file.
    -->

</suite>