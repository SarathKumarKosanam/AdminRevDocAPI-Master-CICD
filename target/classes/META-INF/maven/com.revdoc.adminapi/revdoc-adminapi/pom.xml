<project xmlns="http://maven.apache.org/POM/4.0.0" 
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>com.revdoc.adminapi</groupId>
  <artifactId>revdoc-adminapi</artifactId>
  <version>0.0.1-SNAPSHOT</version>
  
    <properties>
      <!-- CRITICAL: Set Java version properties for consistent compilation (Java 17) -->
      <maven.compiler.source>17</maven.compiler.source>
      <maven.compiler.target>17</maven.compiler.target>
      <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>
  
  
    <dependencies>
    <!-- TestNG: Scope removed, defaults to 'compile'. This is correct because BaseTest.java (in src/main/java) 
         uses TestNG annotations like @BeforeSuite. -->
    <dependency>
        <groupId>org.testng</groupId>
        <artifactId>testng</artifactId>
        <version>7.10.2</version>
    </dependency>

    <!-- Rest Assured: Scope removed, defaults to 'compile'. This is correct because BaseTest.java (in src/main/java) 
         uses RestAssured (RestAssured.baseURI). -->
    <dependency>
        <groupId>io.rest-assured</groupId>
        <artifactId>rest-assured</artifactId>
        <version>5.3.0</version>
    </dependency>

    <!-- ExtentReports: Already correct, defaults to 'compile' which is needed by ExtentManager in src/main/java. -->
    <dependency>
        <groupId>com.aventstack</groupId>
        <artifactId>extentreports</artifactId>
        <version>5.0.9</version>
    </dependency>
</dependencies>

<build>
  <plugins>
    <plugin>
      <groupId>org.apache.maven.plugins</groupId>
      <artifactId>maven-compiler-plugin</artifactId>
      <version>3.11.0</version>
      <configuration>
        <source>17</source>
        <target>17</target>
      </configuration>
    </plugin>
     <!-- CRITICAL: Maven Surefire Plugin: Executes TestNG tests (essential for 'mvn test') -->
    <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.1.2</version>
        <configuration>
            <!-- Points Maven to the testng.xml file for execution -->
            <suiteXmlFiles>
                <suiteXmlFile>testng.xml</suiteXmlFile>
            </suiteXmlFiles>
            <!-- Mandatory: Generate JUnit format reports for the CI reporter -->
            <reportFormat>plain</reportFormat>
            <trimStackTrace>false</trimStackTrace>
        </configuration>
    </plugin>
    
  </plugins>
</build>


  
</project>